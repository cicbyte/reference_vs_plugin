# VS Code 插件：Reference —— 需求文档

## 1. 项目概述

### 1.1 背景
`reference`(https://github.com/cicbyte/reference) 是一款本地代码仓库引用管理器，通过 CLI 将任意 Git 仓库源码与结构化分析知识引入工作项目，为 AI 编程助手（如 Claude Code、Cursor、Copilot 等）提供零延迟、可复用的代码知识库。当前用户只能通过命令行操作，学习成本和操作门槛较高。为降低使用门槛、提升集成体验，需要开发一款 VS Code 插件，将 `reference` 的 CLI 能力图形化整合进编辑器，使开发者无需离开 IDE 即可完成代码引用的添加、浏览、分析与管理。

### 1.2 目标
为 VS Code 提供一款 **reference 的官方图形化前端插件**，核心设计原则是：**不重新实现业务逻辑，仅作为 CLI 二进制文件的调用封装与可视化展现层**。插件将完成以下目标：

- 通过直观的 UI 替代终端命令，降低使用门槛；
- 将 `.reference` 目录下的仓库和知识文件以树形视图呈现，方便浏览；
- 与 VS Code 内置终端、文件浏览器、AI 聊天面板等深度集成；
- 自动检测并管理 `reference` 二进制依赖，提供一键安装引导；
- 保留 CLI 所有高级功能的同时，提供快捷操作和配置面板。

### 1.3 用户角色
| 角色 | 描述 |
|------|------|
| 普通开发者 | 日常使用 AI 编程助手，需快速引用第三方库或内部私有仓库源码 |
| 高级用户 | 了解 `reference` 全部功能，希望通过插件提高操作效率，同时仍可手动执行 CLI |
| 团队管理者 | 关注知识沉淀与复用，希望团队统一使用、配置共享 |

## 2. 功能需求

### 2.1 依赖管理
- **二进制检测与下载**：插件激活时自动检测 `reference` 二进制是否可用（环境变量 PATH 或指定路径）。若未找到，引导用户下载/安装（提供内建下载链接、系统包管理器提示或手动设置路径）。
- **版本管理**：展示当前二进制版本，支持检查更新、一键升级。
- **路径配置**：允许用户在设置中自定义 `reference` 二进制绝对路径。

### 2.2 引用仓库管理（Reference Repos）
- **添加引用**：通过命令面板或侧边栏“+”按钮，输入 Git URL 及引用名称，调用 `reference add`。
- **移除引用**：在列表中选择仓库，调用 `reference remove`，并清理 Junction 链接。
- **列出所有引用**：以树视图展示当前工作区已引用的仓库（从 `.reference/repos/` 扫描或调用 `reference list`）。
- **更新引用**：支持拉取指定仓库最新代码（`reference update`），并可查看日志。
- **快速查看**：支持在 VS Code 中直接打开引用的源码文件（通过 junction 链接路径）；支持以只读、自动补全源码上下文的方式打开。

### 2.3 知识文件管理（Wiki / Knowledge）
- **架构总览**：为指定仓库生成或重新生成 `reference.md`（调 `reference analyze`）。生成完成后自动打开预览。
- **主题知识探索**：针对当前引用仓库，由用户输入自然语言问题，调用 `reference explore` 生成主题知识文件（如“认证流程.md”），并在编辑器中展示或插入当前对话。
- **知识文件浏览**：侧栏树视图展示 `.reference/wiki/<repo>/` 下的所有 `.md` 文件，支持创建、删除、重命名、刷新。
- **知识文件预览**：点击直接打开 Markdown 预览，支持引用内的符号跳转到对应源码位置（需实现路径映射）。
- **AI 聊天集成（关键）**：右键知识文件 →“发送给 Copilot / Cursor Chat”，将文件内容附加为上下文；提供“使用 Explorer 提问”命令，在聊天中自动激活 Explorer 子代理。

### 2.4 统计与分析
- **代码统计**：在仓库节点右键或命令面板触发 `reference repo scc`，以 Webview 表格/图表展示语言占比、复杂度等统计结果。
- **报告面板**：类似内置 Markdown 预览的 Webview，渲染统计信息和知识文件的高级视图，支持导出 HTML。

### 2.5 配置与诊断
- **全局缓存管理**：显示全局缓存路径 `~/.cicbyte/reference/` 的磁盘占用、仓库数量；提供清理未使用缓存的入口。
- **日志与诊断**：收集 `reference` CLI 执行日志，用于故障排查；一键复制诊断信息。
- **设置面板**：提供图形化设置，映射 `reference` 的配置文件或环境变量，如默认远程协议、并发数、代理设置等。

### 2.6 状态指示与通知
- **同步状态**：侧栏底部状态栏显示“reference: 已就绪”或“未找到二进制”；添加引用时显示进度条。
- **后台任务**：长时间操作（如 clone 大仓库）使用 VS Code 进度通知，可取消。

## 3. 用户界面设计

### 3.1 活动栏图标与侧栏容器
- 在活动栏（Activity Bar）添加 Reference 图标（一本打开的书与链接符号）。
- 主侧边栏为一个 TreeView，由三部分组成：
  1. **引用的仓库**：列出当前工作区所有引用仓库名，每个仓库可展开显示分支/HEAD 信息，双击打开 `reference.md`。
  2. **知识文件**：按仓库分组的 Markdown 知识文件列表，支持文件图标。
  3. **快速操作**：一组按钮（添加引用、生成分析、探索提问、查看统计）。

### 3.2 命令面板集成
所有核心命令注册到 VS Code 命令面板（`Ctrl+Shift+P`），以 `Reference:` 为前缀：

| 命令 ID | 标题 | CLI 映射 |
|----------|------|----------|
| `reference.addRepo` | Reference: Add Repository | `reference add <url> <name>` |
| `reference.removeRepo` | Reference: Remove Repository | `reference remove <name>` |
| `reference.updateRepo` | Reference: Update Repository | `reference update <name>` |
| `reference.analyze` | Reference: Generate Architecture Overview | `reference analyze <name>` |
| `reference.explore` | Reference: Explore Topic (Ask a Question) | `reference explore <name> "<question>"` |
| `reference.listRepos` | Reference: List Referenced Repositories | `reference list` |
| `reference.browseCache` | Reference: Open Global Cache Directory | (系统文件管理器) |
| `reference.viewStats` | Reference: Show Code Statistics for Repo | `reference repo scc <name>` |
| `reference.diagnostics` | Reference: Show Diagnostics & Logs | (查看日志) |

### 3.3 编辑器集成
- 知识文件（`.md`）在 VS Code 编辑器中打开时，提供特殊的 **Reference Preview** 编辑器工具栏，包含“刷新知识”、“提问更深层”、“复制到聊天”等按钮。
- 支持单击知识文件内的代码引用路径跳转到缓存的源文件（通过 junction 链接）。

### 3.4 状态栏条目
- 状态栏左侧显示当前工作区引用的仓库数量，点击弹出快速选择菜单。
- 后台同步时显示同步动画。

## 4. 技术方案

### 4.1 架构概览
插件本身不包含任何 `reference` 业务逻辑，仅作为 **CLI wrapper**。

```
VS Code Extension (TypeScript)
├── UI: TreeViews, Webviews, StatusBar, QuickPicks
├── Service Layer
│   ├── ReferenceBinaryManager: 二进制查找、安装、版本校验
│   ├── ReferenceCLI: 封装所有 CLI 命令调用，统一错误处理、输出解析
│   └── WorkspaceManager: 读取/写入 .reference 目录结构，文件监听
└── Adapter: 将 CLI 输出转化为 UI 显示模型
```

### 4.2 二进制调用方式
- 使用 Node.js `child_process.execFile` 直接执行 `reference` 二进制。
- 所有命令需支持 `--json` 输出模式（若 CLI 支持）以便解析；否则解析纯文本/表格输出，或直接通过 `.reference/` 文件系统状态反推（作为后备）。
- 对于长时间操作（如 `clone`），使用 `spawn` 并流式输出到 Output Channel，同时在 UI 显示进度。

### 4.3 状态同步
- 文件监听：监听 `.reference/repos/`、`.reference/wiki/` 目录变化（通过 `vscode.workspace.createFileSystemWatcher`），自动刷新树视图。
- 同步锁：防止用户在 CLI 和插件间并发操作造成冲突，操作前检查是否有 running process。

### 4.4 平台兼容性
- 支持 Windows、macOS、Linux。
- 处理 Junction 链接（Windows）与符号链接（Unix）的显示与操作差异。
- 二进制安装需适配不同系统的默认路径和权限。

### 4.5 依赖
- **运行时依赖**：用户机器上需有 `reference` 二进制（插件可引导安装）。
- **开发依赖**：`@types/vscode`、`typescript`、`esbuild` 等标准 VS Code 扩展脚手架。
- **可选依赖**：为生成图表（代码统计），可能内嵌轻量级 Webview 模板，无外部依赖。

## 5. 非功能性需求

- **性能**：插件启动时间 < 200ms，调用 CLI 的非网络操作应在 2 秒内响应，大仓库 clone 的进度反馈应实。
- **可用性**：所有功能均可通过鼠标或键盘完全操作，所有命令描述清晰并附带图标。
- **可靠性**：CLI 调用失败时，提供人类可读的错误消息，并附上原始日志链接；不因单次失败导致插件崩溃。
- **国际化**：初始版本支持英语和中文，关键提示词预留 i18n 接口。
- **安全**：不收集任何用户数据或仓库信息；二进制下载使用 HTTPS 校验哈希。

## 6. 用户场景示例

### 场景 1：首次使用，快速引用并分析一个库
1. 开发者打开项目，点击活动栏 Reference 图标，看到“未检测到 reference 二进制”的通知，点击“一键安装”，插件下载并配置完成。
2. 点击侧栏“+”按钮，输入仓库 URL `github.com/go-git/go-git`，命名为 `go-git`，确认后后台开始克隆，状态栏显示进度。
3. 克隆完成后，右键该仓库节点 → “Generate Architecture Overview”，系统调用 `reference analyze go-git`，稍后在编辑器自动打开 `reference.md` 预览。
4. 开发者阅读架构概览，快速理解项目结构。

### 场景 2：用 Explorer 解决特定问题
1. 开发者在写代码时需要了解 `go-git` 如何实现 SSH 认证。
2. 在侧栏中右键 `go-git` 仓库 → “Explore Topic”，输入问题：“How does SSH authentication work?”。
3. 插件调用 `reference explore go-git "How does SSH authentication work?"`，后台 Explorer 子代理完成搜索并生成 `SSH-authentication.md`。
4. 文件自动打开，开发者阅读后右键选择“Send to Copilot Chat”，在聊天中直接引用此知识。

### 场景 3：浏览与统计
1. 开发者在团队会议上想展示技术方案，右键仓库节点 → “View Code Statistics”，Webview 弹出语言分布饼图和复杂度统计。
2. 通过全局缓存管理清理三个月未使用的引用，释放磁盘空间。

## 7. 里程碑与版本规划

| 版本 | 范围 | 核心交付 |
|------|------|----------|
| 0.1.0 预览版 | 基础依赖管理 + 添加/删除/列出引用 + 树形视图 | 可用的 CLI 图形化原型 |
| 0.2.0 | 知识文件浏览、`reference.md` 自动生成与预览 | 知识管理闭环 |
| 0.3.0 | Explorer 主题探索 + AI 聊天集成 | 与 AI 助手的紧密协作 |
| 0.4.0 | 代码统计、全局缓存管理、配置面板 | 高级功能与诊断 |
| 1.0.0 | 完整测试、国际化、文档、市场发布 | 生产就绪 |

## 8. 风险与应对

| 风险 | 缓解措施 |
|------|----------|
| CLI 无 `--json` 输出导致解析困难 | 先通过文件系统解析（如读 SQLite 库），同时向 reference 上游提交 JSON 输出 PR |
| 二进制下载在某些网络环境不可用 | 允许用户手动指定二进制路径，提供离线安装包 |
| Windows Junction 权限问题 | 引导用户以管理员权限运行 VS Code，或提供退化为普通拷贝的选项 |
| 后台 clone 大仓库时 VS Code 重载导致进程挂起 | 使用 detached process 和 pid 文件追踪，重启后恢复 |

## 9. 成功指标
- 安装后 30 天内活跃用户中，使用插件完成 3 次以上 `reference add` 操作的比例 > 60%。
- 用户通过插件生成知识文件并用于 AI 聊天的周活跃操作量。
- VS Code 市场评分 4.5 星以上。